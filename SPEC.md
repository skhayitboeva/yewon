# 예원예술대학교 유학생 관리 시스템 — 기능 명세서 (v1.0)

**작성일** 2026-09-19 · **스택** React (Vite) + Netlify Functions + MongoDB Atlas · **UI 언어** 한국어

---

## 1. 개요

기존 참고용 HTML 페이지(`TalkFile_우즈베키스탄_학생관리_업무기록_시스템.html`)는 데이터를 브라우저 `localStorage`에 저장하는 단일 파일 프로토타입이다. 여러 담당자가 같은 데이터를 보고 수정할 수 없고, 브라우저를 지우면 기록이 사라진다.

이 명세는 그것을 **공용 웹 앱**으로 다시 만든다.

| | 기존 프로토타입 | 신규 시스템 |
|---|---|---|
| 저장소 | 브라우저 localStorage | MongoDB Atlas |
| 접근 | 파일 각자 보관 | Netlify URL, 공용 비밀번호 로그인 |
| 탭 | 7개 (대시보드/학부생/대학원생/업무기록/상담기록/비자/등록금/출석) | **2개 (대시보드 · 전체 학생)** |
| 편집 | 일부 필드만 | 표 안에서 전 항목 직접 수정 |
| 학생 추가 | 불가 | 추가 모달 |

**초기 데이터** — 담당자가 제공할 **엑셀 파일**을 MongoDB로 이관한다 (아래 9장). 참고 파일에서 추출한 학생 551명(학부 468 · 대학원 83)은 `data/students.seed.json`으로 이미 정규화해 두었고, 엑셀이 준비되기 전까지 **개발·테스트용 임시 데이터**로 사용한다.

---

## 2. 범위

### 포함
- 대시보드 (전체/등록금/출결/상담 집계)
- 전체 학생 단일 표: 검색 · 필터 · 정렬 · 표 내 직접 편집
- 학생 추가 모달
- 상담 기록 모달 (학생별 이력 + 신규 작성)
- 공용 비밀번호 로그인
- 엑셀(CSV) 내보내기

### 제외 (요청에 따라 삭제)
- 업무 기록 탭, 상담 기록 탭, 비자 관리 탭, 등록금 관리 탭, 출석 처리 탭
- **비자 만료일** 필드 — 완전 삭제 (상담 분야로서의 "비자"는 유지)

---

## 3. 데이터 모델 (MongoDB)

### 3.1 `students` 컬렉션

```jsonc
{
  "_id": ObjectId,
  "studentId":     "20240319",     // 학번 (unique)
  "level":         "학부",          // 학부 | 대학원
  "nameKo":        "카디야틸로",     // 성명
  "nameEn":        "ASATILLAEV KHADYATILLO RAKHMATULLO UGL",  // 성명(영문) ← 신규 표시
  "birthDate":     "2004-08-16",   // 생년월일 ← 신규 표시
  "gender":        "남",            // 남 | 여
  "grade":         "3",            // 학년 (학부생)
  "semesterNo":    "",             // 학기차 (대학원생)
  "enrollStatus":  "재학",          // 학적
  "major":         "경영전공",       // 전공
  "faculty":       "국제매니지먼트학부",
  "gradSchool":    "",             // 대학원
  "course":        "",             // 석사과정 | 박사과정
  "admissionDate": "2024-03-04",   // 입학일자
  "admissionType": "신입학",        // 신입학 | 편입학
  "studentType":   "재학생",        // 신입생 | 재학생  ← 담당자가 직접 선택 (자동 계산 아님)
  "nationality":   "우즈베키스탄",
  "address":       "충북 음성군 …",  // 주소1+주소2 병합 ← 신규 표시 (location)
  "phone":         "",             // 전화번호
  "mobile":        "010-5894-4441",// 휴대전화 ← 신규 표시
  "email":         "",
  "lastRegYear":   "2024",         // 최근등록년도
  "lastRegSemester": "1",          // 최근등록학기

  "tuition": {                     // 등록금 — 전부 수기 입력
    "status": "미납",               // 완납 | 부분납부 | 미납
    "total":  0,                   // 총액 (KRW)
    "term1":  0, "term2": 0, "term3": 0, "term4": 0,
    "note":   ""
  },
  "attendance": {                  // 출결 — 수기 입력
    "absences": 0,                 // 결석 횟수
    "note": ""
  },
  "contactCount": 0,               // 연락횟수 ← 신규, 수기 입력
  "memo": "",

  "createdAt": ISODate, "updatedAt": ISODate, "updatedBy": "shared"
}
```

**인덱스**
```
{ studentId: 1 }        unique
{ level: 1, nameKo: 1 }
{ major: 1 }
{ "tuition.status": 1 }
{ "attendance.absences": 1 }
{ admissionDate: 1 }
{ nameKo: "text", nameEn: "text", studentId: "text", mobile: "text" }
```

### 3.2 `consultations` 컬렉션

```jsonc
{
  "_id": ObjectId,
  "studentId": "20240319",
  "date": "2026-09-19",
  "categories": ["등록금", "출석"],   // 체크박스 다중 선택
  "method": "대면",                  // 대면 | 전화 | 카카오톡 | 이메일 | 기타
  "content": "2차 등록금 납부 일정 안내",
  "result": "10/15까지 납부 약속",
  "counselor": "",
  "createdAt": ISODate, "updatedAt": ISODate
}
```

**상담 분야 (체크박스 6종, 요청 그대로)**
`등록금` · `출석` · `비자` · `학업 관련(출석 포함)` · `긴급` · `기타`

**인덱스** `{ studentId: 1, date: -1 }`, `{ date: -1 }`, `{ categories: 1 }`

### 3.3 `settings` 컬렉션 (단일 문서)

```jsonc
{ "_id": "app", "currentYear": 2026, "currentSemester": 2 }
```
대시보드의 "신입생" 기준 학기. 화면에서 변경 가능 → 다음 학기에 코드 수정 없이 넘어간다.

---

## 4. 대시보드

### 4.1 전체 학생

| 카드 | 정의 |
|---|---|
| **전체 학생** | `students` 전체 건수 |
| **학부 — 재학생** | `level=학부` AND `studentType=재학생` |
| **학부 — 신입생** | `level=학부` AND `studentType=신입생` |
| **대학원 — 재학생** | `level=대학원` AND `studentType=재학생` |
| **대학원 — 신입생** | `level=대학원` AND `studentType=신입생` |

`studentType`(신입생 / 재학생)은 **자동 계산하지 않는다.** 담당자가 전체 학생 표에서 직접 선택한다:

- 표의 **신입/재학** 컬럼이 드롭다운이며, 한 명씩 바꿀 수 있다.
- 행 선택(체크박스) 후 **일괄 변경** 버튼으로 여러 명을 한 번에 신입생/재학생으로 지정할 수 있다 → 학기가 바뀔 때 수백 명을 한 번에 처리.
- 표에서 **입학 코호트(입학일자 기준)** 필터로 먼저 걸러낸 뒤 일괄 변경하면 편하다. 참고용 분포:

| 입학 코호트 | 학부 | 대학원 | 계 |
|---|---:|---:|---:|
| 2024-1 | 7 | 1 | 8 |
| 2025-1 | 0 | 4 | 4 |
| 2025-2 | 77 | 27 | 104 |
| 2026-1 | 132 | 24 | 156 |
| 2026-2 | 252 | 27 | 279 |
| **계** | **468** | **83** | **551** |

대시보드 카드 제목에는 `settings`의 현재 학기(예: `2026-2학기`)가 표기되지만, 집계 자체는 `studentType` 값만 본다.

### 4.2 등록금

| 지표 | 정의 |
|---|---|
| 완납 | `tuition.status = "완납"` |
| 미납 | `tuition.status = "미납"` |
| 부분납부 | `tuition.status = "부분납부"` |
| 1차 납부 | `tuition.term1 > 0` |
| 2차 납부 | `tuition.term2 > 0` |
| 3차 납부 | `tuition.term3 > 0` |
| 4차 납부 | `tuition.term4 > 0` |

각 카드에 건수 + 전체 대비 %. 등록금 카드에는 총 고지액 / 총 수납액 / 미수금 합계도 표시.

**카드 클릭 → 전체 학생 탭으로 이동하며 해당 필터가 적용된다.** (모든 대시보드 카드 공통)

### 4.3 출결

| 구간 | 정의 |
|---|---|
| 양호 | `absences = 0` |
| 결석 1회 | `absences = 1` |
| 결석 2–3회 | `absences` 2~3 |
| **결석 4회 이상 (F 대상)** | `absences >= 4` — 빨간색 강조 |

### 4.4 상담

상담 분야별 **기록 건수**와 **해당 학생 수**를 함께 표시: 비자 / 등록금 / 출석 / 학업 관련(출석 포함) / 긴급 / 기타. 최근 상담 5건 목록 포함.

---

## 5. 전체 학생 탭

### 5.1 표 컬럼

| # | 컬럼 | 편집 | 정렬 | 비고 |
|---|---|---|---|---|
| 1 | 학번 | ✎ | ✓ | 고정 열 (좌측 sticky) |
| 2 | 성명 | ✎ | ✓ | 고정 열 |
| 3 | **성명(영문)** | ✎ | ✓ | **신규** |
| 4 | **생년월일** | ✎ date | ✓ | **신규** |
| 5 | 성별 | ✎ select | ✓ | |
| 6 | 구분 | ✎ select | ✓ | 학부 / 대학원 — **헤더 클릭 정렬** |
| 7 | 소속 | ✎ | ✓ | 학부 또는 대학원 |
| 8 | 전공 | ✎ | ✓ | **헤더 클릭 정렬** |
| 9 | 학년/학기차 | ✎ | ✓ | |
| 10 | 학적 | ✎ select | ✓ | **헤더 클릭 정렬** |
| 11 | **신입/재학** | ✎ select | ✓ | **신규** — 신입생 / 재학생, 수동 선택 · 일괄 변경 지원 |
| 12 | 입학구분 | ✎ select | ✓ | 신입학 / 편입학 |
| 13 | 입학일자 | ✎ date | ✓ | |
| 14 | **주소 (location)** | ✎ | ✓ | **신규** |
| 15 | **휴대전화** | ✎ | ✓ | **신규** |
| 16 | 이메일 | ✎ | ✓ | |
| 17 | **등록금** | ✎ 확장 | ✓ | 상태 드롭다운 + 총액/1~4차 금액 — 아래 5.2 |
| 18 | **결석(출석)** | ✎ number | ✓ | **헤더 클릭 정렬** |
| 19 | **연락횟수** | ✎ number | ✓ | **신규** |
| 20 | **상담** | 버튼 | — | 기록 건수 배지 + 모달 열기 |

- 삭제된 컬럼: **비자 만료일**
- 열 표시/숨김 토글 메뉴 제공 (컬럼이 많아 기본은 핵심 10개만 노출, 나머지는 체크로 켬)
- 정렬: 헤더 클릭 → 오름차순 → 내림차순 → 해제. 한글은 `ko-KR` 로케일 정렬, 숫자/날짜는 타입별 정렬.

### 5.2 등록금 셀

셀에 요약이 보이고(`부분납부 · 1,500,000 / 3,500,000`), 클릭하면 인라인 패널이 열린다:

```
상태   [ 완납 ▾ ]   ← 완납 / 부분납부 / 미납
총액   [        3,500,000 ]
1차    [        1,500,000 ]
2차    [                0 ]
3차    [                0 ]
4차    [                0 ]
비고   [                  ]
                  [취소] [저장]
```

다섯 금액 모두 **수기 입력**, 상태도 **수기 선택**(요청대로 자동 계산 없음). 다만 입력한 합계가 총액과 맞지 않으면 안내 문구만 표시하고 저장은 막지 않는다. 금액은 천 단위 콤마, 원 단위.

### 5.3 검색 · 필터

- **검색창** — 학번 / 성명 / 영문명 / 휴대전화 동시 검색
- **구분** 전체 · 학부 · 대학원
- **신입/재학** 전체 · 신입생 · 재학생
- **전공** 드롭다운 (실제 값에서 자동 생성)
- **등록금 상태** 전체 · 완납 · 부분납부 · 미납
- **납부 차수** 1차~4차 납부 여부
- **출결** 전체 · 양호 · 1회 · 2–3회 · 4회 이상
- **입학 코호트** 2024-1 … 2026-2
- 필터 상태는 URL 쿼리에 저장 → 링크 공유·새로고침에도 유지
- 필터 결과 **CSV 내보내기** 버튼

### 5.4 표 내 편집

- 셀 클릭 → 입력 활성화. `Enter`/포커스 아웃 = 저장, `Esc` = 취소
- 낙관적 업데이트(즉시 화면 반영) → 실패 시 원복 + 토스트 알림
- 저장 시 `PATCH /api/students/:id` 로 **변경 필드만** 전송
- 페이지네이션 50건/페이지, 페이지 크기 변경 가능

**일괄 변경** — 행 체크박스로 여러 명 선택 → 상단 바에 `N명 선택됨` 표시 → 다음을 한 번에 적용:
- 신입/재학 지정 (신입생 ↔ 재학생)
- 학적 변경 (재학 / 휴학 / 제적 …)
- 등록금 상태 변경
- 현재 필터 결과 **전체 선택** 지원 → `PATCH /api/students/bulk`

### 5.5 학생 추가 모달

모든 기본 항목 입력. **학번 필수 + 중복 검사**(서버 unique 인덱스 + 저장 전 확인). 등록금/출결/연락횟수는 0으로 시작.

### 5.6 상담 기록 모달

학생 행의 **상담** 버튼으로 열린다.

**상단 — 신규 기록 작성**
- 일자 (기본: 오늘)
- 분야 체크박스 — ☐ 등록금 ☐ 출석 ☐ 비자 ☐ 학업 관련(출석 포함) ☐ 긴급 ☐ 기타
- 상담 방법 (대면/전화/카카오톡/이메일/기타)
- 상담 내용 (자유 입력, 여러 줄)
- 조치/결과 (자유 입력, 여러 줄)
- 담당자

**하단 — 해당 학생의 기록 이력** 최신순, 각 건 수정·삭제 가능.

---

## 6. API (Netlify Functions)

인증 쿠키 필수. 모든 응답 JSON.

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/login` | `{password}` → httpOnly 세션 쿠키 발급 |
| POST | `/api/logout` | 쿠키 삭제 |
| GET | `/api/me` | 세션 유효성 확인 |
| GET | `/api/stats` | 대시보드 집계 (단일 aggregation) |
| GET | `/api/students` | `q, level, major, tuitionStatus, term, absenceBucket, cohort, sort, dir, page, limit` |
| POST | `/api/students` | 학생 추가 |
| PATCH | `/api/students/:id` | 부분 수정 |
| PATCH | `/api/students-bulk` | 선택/필터 대상 일괄 수정 (신입·재학, 학적, 등록금 상태) |
| GET | `/api/facets` | 필터 드롭다운 값 (전공 · 입학 코호트) |
| DELETE | `/api/students/:id` | 삭제 |
| GET | `/api/consultations?studentId=` | 학생별 상담 이력 |
| POST | `/api/consultations` | 상담 기록 추가 |
| PATCH | `/api/consultations/:id` | 수정 |
| DELETE | `/api/consultations/:id` | 삭제 |
| GET | `/api/settings` · PUT | 현재 학기 설정 |
| GET | `/api/export.csv` | 현재 필터 기준 CSV |

**서버 공통** — MongoDB 연결은 모듈 스코프에 캐시(Lambda 컨테이너 재사용), 요청마다 새 연결을 만들지 않는다. 입력은 Zod로 검증하고, 스키마에 없는 필드는 버린다.

---

## 7. 인증

- 공용 비밀번호 1개. 평문이 아니라 **scrypt 해시**를 환경변수에 넣는다 (`APP_PASSWORD_HASH`).
- 로그인 성공 → HS256 JWT를 `HttpOnly; Secure; SameSite=Lax; Max-Age=12h` 쿠키로 발급.
- 모든 API가 쿠키를 검증. 실패 시 401 → 프론트는 로그인 화면으로.
- 로그인 시도 제한: 동일 IP 10회 실패 시 15분 차단 (MongoDB에 기록).
- 개인정보(생년월일·주소·연락처)가 있으므로 `noindex` 메타 + `robots.txt` 차단.

---

## 8. 기술 스택

**프론트엔드**
- React 19 + TypeScript + Vite
- TanStack Query (서버 상태·캐시·낙관적 업데이트)
- Tailwind CSS
- 표는 직접 구현 (551건 규모에서는 가상 스크롤 불필요, 페이지네이션으로 충분)
- 한국어 폰트: Pretendard

**백엔드**
- Netlify Functions v2 (Node 20, ESM)
- `mongodb` 공식 드라이버
- `jose` (JWT), `zod` (검증)

**DB** — MongoDB Atlas M0(무료) 또는 M10. 네트워크 접근은 `0.0.0.0/0` 허용 필요 (Netlify는 고정 IP 없음) — 그래서 DB 사용자 비밀번호를 길게 잡는다.

---

## 9. 배포

```
yewon-sms/
├─ src/                    # React 앱
├─ netlify/functions/      # API
├─ data/students.seed.json # 초기 551명
├─ scripts/seed.mjs        # 1회 import
├─ netlify.toml
└─ package.json
```

**netlify.toml**
```toml
[build]
  command = "npm run build"
  publish = "dist"
[functions]
  node_bundler = "esbuild"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

**필요한 환경변수 (Netlify Site settings → Environment variables)**

| 변수 | 내용 |
|---|---|
| `MONGODB_URI` | Atlas 연결 문자열 |
| `MONGODB_DB` | `yewon_sms` |
| `APP_PASSWORD_HASH` | 공용 비밀번호 scrypt 해시 (생성 스크립트 제공) |
| `JWT_SECRET` | 32바이트 랜덤 문자열 |

**배포 순서**
1. Atlas 클러스터 생성 → DB 사용자 → 네트워크 접근 허용 → 연결 문자열 확보
2. `node scripts/seed.mjs` 로 임시 551명 + 인덱스 생성 (로컬에서 1회)
3. GitHub 저장소 연결 또는 `netlify deploy --prod`
4. 환경변수 4개 입력 → 재배포
5. 로그인 후 데이터 확인
6. 엑셀 원본 도착 시 `scripts/import-excel.mjs` 로 실데이터 이관 (아래 9.1)

### 9.1 엑셀 → MongoDB 이관

담당자가 엑셀(.xlsx)을 주면 다음 순서로 처리한다.

1. **컬럼 매핑 확인** — 엑셀 헤더를 읽어 3.1 스키마와 짝지은 매핑표를 먼저 보여주고, 애매한 컬럼은 확인받는다.
2. **검증(dry-run)** — 실제 쓰기 전에 리포트를 낸다: 총 건수, 학번 중복, 학번 누락, 날짜 형식 오류, 기존 DB에 이미 있는 학번 수.
3. **이관 모드 선택**
   - `--mode=replace` : 기존 `students` 비우고 새로 넣기
   - `--mode=upsert` : 학번 기준으로 있으면 갱신, 없으면 추가 *(권장)*
   - `--mode=insert-only` : 새 학번만 추가, 기존 건은 그대로
4. **보존 규칙** — upsert 시 **등록금 · 출결 · 연락횟수 · 메모 · 상담 기록은 덮어쓰지 않는다.** 엑셀에 없는 정보이고, 담당자가 앱에서 입력한 값이기 때문. 학적 정보(이름·전공·연락처 등)만 갱신된다.
5. **롤백** — 이관 직전 `students_backup_YYYYMMDD` 컬렉션으로 전체 스냅샷을 남긴다.

`studentType`(신입생/재학생)은 엑셀에 해당 컬럼이 있으면 가져오고, 없으면 기본값 `재학생`으로 넣은 뒤 앱의 일괄 변경으로 지정한다.

---

## 10. 확인이 필요한 항목

1. **엑셀 원본** — 컬럼 매핑을 확정하려면 파일이 필요합니다. (이관 전까지는 참고 파일 551명으로 개발)
2. **등록금 금액 기준** — 학부/대학원 표준 등록금 액수가 정해져 있으면 신규 학생 추가 시 총액 기본값으로 넣어 둘 수 있습니다.
3. **삭제 권한** — 학생 삭제를 허용할지, 아니면 `학적`을 `제적/휴학`으로 바꾸는 방식만 둘지.
4. **Netlify · MongoDB 자격정보** — 배포 단계에서 필요.

---

## 11. 작업 순서

| 단계 | 내용 | 산출물 |
|---|---|---|
| 1 | 명세 확정 | 이 문서 |
| 2 | 프로젝트 스캐폴드 + DB 스키마/인덱스 | 저장소 구조 |
| 3 | API 함수 + 인증 | `/api/*` 동작 |
| 4 | 대시보드 | 탭 1 |
| 5 | 전체 학생 표 (정렬·필터·인라인 편집) | 탭 2 |
| 6 | 학생 추가 · 상담 모달 | 모달 2종 |
| 7 | 임시 시드 import (551명) | 개발용 데이터 |
| 8 | Netlify 배포 + 검수 | 운영 URL |
| 9 | 엑셀 수령 → 매핑 확인 → 실데이터 이관 | 운영 데이터 |
